parallelize=1
is.numeric(parallelize)
if(parallelize!=1)
{message "hello"}
if(!is.numeric(parallelize))
{
message("Number of Cores needed")
stop()
}
else if(parallelize!=1){availcore=parallelize}
else{availcore=1}
if(!is.numeric(parallelize))
{
message("Number of Cores needed")
stop()
}
else
{
if(parallelize!=1){availcore=parallelize}
else{availcore=1}
}
if(!is.numeric(parallelize))
{
message("Number of Cores needed")
stop()
}
else if(parallelize!=1){availcore=parallelize}
availcore=1
if(!is.numeric(parallelize))
{
message("Number of Cores needed")
stop()
}
if(parallelize!=1){availcore=parallelize}
###################################
availcore=1
if(!is.numeric(parallelize))
{
message("Number of Cores needed")
stop()
}
if(parallelize!=1){availcore=parallelize}
parallelize=1
parallelize=4
availcore=1
if(!is.numeric(parallelize))
{
message("Number of Cores needed")
stop()
}
else{availcore=parallelize}
if(!is.numeric(parallelize))
{
message("Number of Cores needed")
stop()
}
if(!is.numeric(parallelize))
{
message("Number of Cores needed")
stop()
} else{availcore=parallelize}
availcore=1
if(!is.numeric(parallelize))
{
message("Number of Cores needed")
stop()
}
else{availcore=parallelize}
###################################
availcore=1
if(!is.numeric(parallelize))
{
message("Number of Cores needed")
stop()
}else{availcore=parallelize}
availcore
